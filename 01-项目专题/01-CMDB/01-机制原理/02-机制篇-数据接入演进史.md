# 第二卷 · 机制篇 B · 数据接入演进史（本卷灵魂章）

> **本章定位**：**全书最重要的一章**——把 TCUM CMDB 的数据接入从**"死模型单表"**演进到**"自定义模型 + FlinkCDC + AI 生成"**的完整六阶段讲透。这是候选人**面试可以打的最大王牌**——阶段 4 是候选人亲手做的，阶段 6 是候选人的下一步判断。
>
> **本章金句**：
> - **"从 X 人月接入到接近 0 人月接入" —— 六阶段演进的量化价值**
> - **"CMDB 不主动调用 Flink 平台，只生成 SQL 和 DDL 落库"**（iwiki 4024143170 明示）—— 阶段 6 的独特设计
> - **"每个阶段都解了上个阶段的问题，但引入了新问题" —— 演进的本质**

---

## 📖 本章目录

- **§1** 命题：为什么数据接入需要六阶段演进
- **§2** 阶段 1：单表模式（起点·痛点）
- **§3** 阶段 2：抽象元模型（第一次抽象）
- **§4** 阶段 3：直连他人数据库（重耦合的教训）
- **§5** 阶段 4：SPI + Kafka（本人主导·2025 年）
- **§6** 阶段 5：多 region 数据库（跨地域）
- **§7** 阶段 6：FlinkCDC + AI 生成（2026·下一代）
- **§8** 演进曲线：单产品接入成本从 X 人月降到近 0
- **§9** 与业界对比：Netflix Edda / DataHub / ServiceNow

---

# §1 · 命题：为什么数据接入需要六阶段演进

## §1.1 CMDB 的"接入焦虑"

**每年新增 10+ 云产品要接入 CMDB**。如果每接一个都需要 3 人月，一年就要 30 人月——**接入团队被压死**。

而且不接入的代价更大：
- 监控系统查不到元数据 → tag 补全失败 → 数据丢弃
- 告警系统不知道对象归属 → 无法通知
- AI Agent 拓扑查询空白

**核心矛盾**：**接入速度 vs 接入质量**。

## §1.2 六阶段的驱动力

每个阶段都是被**具体现象逼出来**的：

| 阶段 | 被什么逼出来 | 解了什么 | 新问题 |
|---|---|---|---|
| 1 单表 | 起点 | 简单能用 | 字段爆炸、类型冲突 |
| 2 元模型 | 字段爆炸 | 类型安全 | 静态定义、演进僵化 |
| 3 直连 DB | 接入慢 | 接入快 | 强耦合、DBA 半夜改表 |
| 4 SPI+Kafka | 强耦合 | 接入解耦、增量秒级 | 版本管理、消息乱序 |
| 5 多 region | 单 region 崩 | 跨地域、就近读写 | region 一致性、脑裂 |
| 6 FlinkCDC | SPI 需要云产品配合 | 极致接入零门槛 | Flink 状态大、成本 |

## §1.3 完整叙事（面试话术模板）

> "TCUM CMDB 的数据接入经历了 6 个阶段。**每个阶段都是被具体现象逼出来的**：
> - 阶段 1 是单表模式，接一个产品加几十个字段——字段炸了
> - 阶段 2 引入抽象元模型，每类实体独立表——但静态定义演进僵化
> - 阶段 3 直连他人 MySQL，接入快——但**他人 DBA 半夜改表结构，早上告警一片红**
> - 阶段 4 是**我 2025 年主导的 SPI + Kafka 方案**，接入解耦、增量秒级——但引入了消息乱序、版本管理问题（我用 event_time 乐观锁解决）
> - 阶段 5 多 region 数据库，跨地域容灾——但要处理 region 一致性
> - 阶段 6 是 **2026 的 FlinkCDC + AI 生成**，无侵入接入云产品 binlog，AI 生成 Flink SQL 脚本——这是下一代方向。
>
> **量化价值**：单产品接入成本从阶段 1 的 X 人月降到阶段 4 的 Z 人月（Z << X），阶段 6 接近零成本（基建成本转移到 Flink 集群）。"

---

# §2 · 阶段 1：单表模式（起点）

## §2.1 形态

```sql
CREATE TABLE ci_asset (
    id BIGINT PRIMARY KEY,
    ci_type VARCHAR(64),        -- CVM / CDB / Redis / ...
    -- CVM 相关字段
    instance_id VARCHAR(64),
    region VARCHAR(64),
    status VARCHAR(32),
    -- CDB 相关字段
    db_name VARCHAR(128),
    port INT,
    -- Redis 相关字段
    memory_size VARCHAR(32),
    ... 
    -- 一张表几百个字段
);
```

**接入方式**：每接入一个新云产品 = 加几十个 CVM/CDB/Redis 独有字段 + 改所有查询。

**典型代码痕迹**：`cmdb.isd.com` 老仓库（部分逻辑还在）

## §2.2 三大痛点

**痛点 1 · 字段爆炸**
- 接 10 个云产品 → 表有几百个字段
- `ALTER TABLE` 加字段要锁表几分钟
- 查询 `SELECT *` 返回一堆 NULL 字段

**痛点 2 · 类型冲突**
- 同名字段（比如 `status`）在不同产品里有不同枚举值
- 类型系统失效——用户查 `status='RUNNING'` 拿到一堆无关数据

**痛点 3 · 多产品共享表锁**
- CVM 大规模变更 → `UPDATE` 触发表锁
- CDB 团队的写入被阻塞

## §2.3 单产品接入成本估算

**假设**：一个中等复杂度云产品
- schema 设计：5 人日
- 加字段：2 人日
- 写映射代码：10 人日
- 写查询代码：15 人日
- 测试：5 人日
- **合计**：≈ 1.5 人月

**但真实情况**：
- 涉及跨团队沟通、审批
- ALTER TABLE 需要 DBA 排期
- 影响面测试要跨 CVM/CDB/Redis 全部业务
- **实际耗时**：**3~5 人月**

---

# §3 · 阶段 2：抽象元模型（第一次抽象）

## §3.1 形态

**ModelRegistry 引入**（`cmdb-core`）：
```
model_type=cvm_instance → 表 tcs_cvm_entity_instance + DO 类型 CvmInstance
model_type=cdb_instance → 表 tcs_cdb_entity_instance + DO 类型 CdbInstance
```

**每类实体一张独立表**：
```sql
CREATE TABLE tcs_cvm_entity_instance (
    id BIGINT PRIMARY KEY,
    tenant_id VARCHAR(50),        -- ★ 强制多租户隔离
    sync_version BIGINT,           -- ★ 乐观锁版本号
    sync_source VARCHAR(20),       -- FULL_SYNC / INCR_SYNC
    last_sync_time DATETIME,
    -- CVM 独有字段
    instance_id VARCHAR(64),
    region VARCHAR(64),
    status VARCHAR(32),
    ...
);
```

## §3.2 元模型的核心组件（`INCR_SYNC_DESIGN §5`）

**ModelRegistry**（§5.1）：注册"model_type → 表 + DO"映射

**AttributeConverter**（§5.2）：Protobuf attributes → DO 字段转换

**元字段规范**：所有实体表都必须有 `id / tenant_id / sync_version / sync_source / last_sync_time / created_at / updated_at`

**iwiki 4013370589 强制约束**：**"所有资源实体必须有 tenant_id 做逻辑隔离"**

## §3.3 解决的问题

- ✅ 字段爆炸：每类实体独立表
- ✅ 类型冲突：DO 类型化
- ✅ 共享表锁：写入分散到不同表

## §3.4 引入的新问题

- ❌ **静态定义**：新增字段仍需 `ALTER TABLE`
- ❌ **模型演进僵化**：改字段类型需要迁移所有数据
- ❌ **跨产品关系仍难描述**：CVM 属于哪个 App？外键？关系表？
- ❌ **接入还是要写代码**：每接一个新产品要建表 + 定义 DO + 写映射

## §3.5 单产品接入成本

**降到 ≈ 1 人月**：
- schema 设计：3 人日
- 建表：1 人日
- DO 定义：2 人日
- 映射代码：10 人日
- 测试：4 人日

**仍然痛点**：**跨团队协调**依然是主要成本（20~50% 的时间）。

---

# §4 · 阶段 3：直连他人数据库（重耦合的教训）

## §4.1 形态

**为了加速接入**，CMDB 直接连各云产品的 MySQL replica：

```yaml
# CMDB 侧配置
data_sources:
  - name: cvm_direct
    type: DIRECT_DB           # ★ 代码中的枚举（iwiki 4024143170 明示 DataSourceType.DIRECT_DB）
    dsn: "user:pwd@tcp(cvm-replica.woa.com:3306)/cvm_prod"
  - name: cdb_direct
    type: DIRECT_DB
    dsn: "user:pwd@tcp(cdb-replica.woa.com:3306)/cdb_prod"
```

**代码证据**（`controller/company_cmdb_proxy_controller.go` + `controller/companycmdbcontroller/`）：TCUM CMDB 直接代理到公司 CMDB。

## §4.2 一段时间的甜蜜期

**接入方式**：
- 云产品给个 read-only replica 账号
- CMDB 侧写 SQL 映射到本地表
- **接入时间：3 天到 1 周**

**收益**：
- 接入极快
- 数据实时性好（replica 有几秒延迟）

## §4.3 然后陷阱一个一个来

**陷阱 1 · 他人 DBA 半夜改表结构**

**真实案例**（推演）：
- 12:00 AM：CVM DBA 加了个 `gpu_type` 字段（正常运维）
- 4:00 AM：CVM DBA 把 `status` 字段类型从 `VARCHAR(32)` 改成 `INT`（业务重构）
- 7:00 AM：TCUM 值班收到"CMDB CVM 数据全部无法解析"告警
- **根因**：CMDB 侧代码期望 `status` 是字符串，突然变 INT 崩溃

**教训**：**直连他人 DB = 与他人 schema 强耦合**。他人升级即断链。

**陷阱 2 · 跨部门权限治理**

- 每个云产品的 replica 账号要跨部门审批
- 权限变更、密码轮换、DBA 换人都要重新申请
- **接入 10 个云产品 = 10 套账号 = 10 倍权限运维**

**陷阱 3 · 性能相互干扰**

- CMDB 侧对 CVM replica 跑大聚合查询
- CVM 的其他消费方（比如 CVM 前端）也在读同一个 replica
- **一个大查询 → 全体消费方卡顿**

**陷阱 4 · 无幂等保证**

- CMDB 侧读到的是"当前时刻的数据快照"
- 中间可能 in-flight transaction 未提交
- **CMDB 拿到的数据可能是"脏读"**

**陷阱 5 · 无变更事件**

- 直连 replica 只能"轮询变化"（`WHERE updated_at > last_sync`）
- **无法感知 DELETE**（记录已经不在了）
- 需要业务侧配合软删除标记

## §4.4 单产品接入成本

**表面上降到 3~5 人日**——但**隐藏成本极高**：
- 长期运维成本（他人升级即断链）
- 跨团队权限成本
- 数据质量成本（脏读、DELETE 感知）

## §4.5 阶段 3 的历史遗迹

**代码里仍然存在**：
- `controller/company_cmdb_proxy_controller.go` —— 公司 CMDB 代理
- `controller/companycmdbcontroller/` —— 公司 CMDB 交互
- `service/kafka/consumer/company_cmdb_kafka_consumer.go`（246 行）—— **老一代 Kafka 消费者，用于处理公司 CMDB 直连场景**

**面试可以讲**：**"这是历史包袱，老 case 还在跑，新数据源都走阶段 4 的统一 SPI + Kafka"**。

---

# §5 · 阶段 4：SPI + Kafka（本人主导·2025 年）

## §5.1 形态

**核心设计**（`INCR_SYNC_DESIGN.md` 本人 813 行文档）：

### 全量同步（SPI Pull）
```
CMDB → GET /cmdb/spi/entities?type=X&updated_since=Y&page=Z
云产品侧返回统一 JSON
CMDB doDiff(local, remote) → insert/update/delete
```

### 增量同步（Kafka Push）
```
云产品变更 → 生产 SyncMessage (Protobuf) → Kafka
CMDB UnifiedIncrSyncConsumer 消费
AOP 版本控制中间件检查 event_time > current sync_version
DAO 执行 SQL (带 WHERE sync_version < ? 兜底)
manual commit Kafka offset
```

**Kafka Consumer 配置**（`unified_incr_sync_consumer.go:56-70`，代码级证据）：
```go
kafkaConfig := &kafka.ConfigMap{
    "bootstrap.servers":  config.Cfg.Kafka.IncrSyncConsumerServers,
    "group.id":           config.Cfg.Kafka.IncrSyncConsumerGroupId,
    "auto.offset.reset":  "latest",           // 从最新开始，不回溯历史
    "security.protocol":  "plaintext",
    "session.timeout.ms": 10000,
    "request.timeout.ms": 30000,
    "socket.timeout.ms":  30000,
    "enable.auto.commit": false,              // ★★★ 明确 manual commit
}
```

**这些参数值都是可背下来的**——面试问 "Kafka 消费者怎么配的" 有具体答案。

## §5.2 四大设计支柱（`INCR_SYNC_DESIGN §二`）

**支柱 1 · 统一消息协议**（§2.1）
```protobuf
message SyncMessage {
  string trace_id = 1;
  OperationType operation = 2;     // CREATE/UPDATE/DELETE/BATCH_UPSERT
  string model_type = 3;
  google.protobuf.Timestamp event_time = 4;
  string source = 5;
  oneof payload {
    CreatePayload create = 10;
    UpdatePayload update = 11;
    DeletePayload delete = 12;
    BatchPayload batch = 13;
  }
}
```

**支柱 2 · 表驱动架构**（§2.2）
- `model_type` → ModelRegistry → 表名 + DO 类型
- **无需为每个表写单独处理逻辑**
- 代码证据：`service/modelservice/` 下 50+ 个 service，每个一个云产品实体

**支柱 3 · 版本控制**（§2.3）
- **乐观锁**：`sync_version = event_time.UnixMilli()`
- **非侵入 AOP**：CMDB 内部维护版本，不依赖外部字段
- **三钩子**：`BeforeCreate` / `BeforeUpdate` / `BeforeDelete`

**支柱 4 · 最终一致性**（§2.4）
- **不用分布式锁**
- 通过版本号保证最终一致性
- 允许短暂的数据不一致（cmdb-check 兜底）

## §5.3 三大问题场景（`INCR_SYNC_DESIGN §4.1`）

| 场景 | 描述 | 风险 |
|---|---|---|
| 1 | 全量同步时收到增量消息 | 增量数据被全量数据覆盖 |
| 2 | 增量消息乱序到达 | 旧数据覆盖新数据 |
| 3 | 全量同步覆盖更新的增量数据 | 丢失最新变更 |

**解决方案统一是 sync_version 乐观锁**：

```sql
-- 关键 SQL（§4.3 原文）：
UPDATE tcs_redis_entity_instance
SET instance_name = ?, port = ?,
    sync_version = ?, sync_source = 'INCR_SYNC', last_sync_time = NOW()
WHERE id = ?
  AND (sync_version < ? OR sync_version IS NULL);   -- ★ 关键护栏
```

**乱序场景举例**：
- T0：event_time=100 UPDATE 消息 A
- T0+3s：event_time=90 UPDATE 消息 B（乱序）
- 消费 A：sync_version → 100
- 消费 B：`WHERE sync_version < 90` → 当前是 100，跳过
- **结果**：以最新（event_time=100）的数据为准 ✓

## §5.4 全量与增量共存

**全量同步的 sync_version 语义**：
```go
syncVersion := time.Now().UnixMilli()  // 同步开始时间
for _, data := range insertList {
    data["sync_version"] = syncVersion
    data["sync_source"] = "FULL_SYNC"
}
// UPDATE 时：WHERE id = ? AND (sync_version < ? OR sync_version IS NULL)
```

**关键**：全量启动时的 `syncVersion` 是**同步开始时间**（快照）。如果这期间增量消息（event_time=X > syncVersion）先到，全量的 UPDATE 会被跳过——**增量数据得以保留**。

## §5.5 五大核心组件（`INCR_SYNC_DESIGN §五`）

| 组件 | 文件 | 职责 |
|---|---|---|
| **ModelRegistry** | modelservice/ | `model_type` → 表名 + DO 类型 |
| **AttributeConverter** | modelservice/ | SyncMessage attributes → DO 字段 |
| **VersionControlMiddleware** | AOP | 拦截 DAO 层 BeforeCreate/Update/Delete |
| **IncrSyncService** | bizservice/incr_sync_service/ | 增量同步业务逻辑 |
| **UnifiedIncrSyncConsumer** | kafka/consumer/ | **本人代码，272 行** |

## §5.6 单产品接入成本

**降到 ≈ 5~10 人日**：
- 云产品方实现 SPI 接口：3 人日
- 云产品方生产 SyncMessage：2 人日
- CMDB 侧注册模型：1 人日（改配置即可）
- 测试：3 人日

**主要成本转移到云产品侧**（他们做适配层）。**这就是"接入解耦"的意义**。

## §5.7 阶段 4 引入的新问题

- ❌ SPI 版本管理（如 SPI 定义升级，老适配层失效）
- ❌ Kafka 消息乱序（乐观锁解，但极端场景仍有边界）
- ❌ event_time 依赖数据源 NTP
- ❌ 单 region Kafka 集群（跨 region 需要阶段 5）

---

# §6 · 阶段 5：多 region 数据库（跨地域）

## §6.1 形态

**代码证据**（iwiki 4024143170 明示）：`DataSourceType` 枚举包含 **`REGION_DB`**——**多 region 数据库**。

**多 region 场景**：
- 一个云产品在多个 region 都有 MySQL
- CMDB 需要**同时消费多个 region 的数据**
- 数据落到 CMDB 后还要**跨 region 展示**

**关键 SQL 特征**（`t_cmdb_custom_flink_cdc_script.covered_regions`）：多 region UNION ALL

```
covered_regions = "ap-guangzhou,ap-beijing,ap-shanghai"
```

## §6.2 挑战

**挑战 1 · region 数据一致性**
- 一份数据在 3 个 region 都存
- 靠 TDSQL DCN 跨地域复制（iwiki 4018944587 底座卷提及）
- **短板**：DCN 有秒到分钟级延迟

**挑战 2 · 脑裂预防**
- region 网络抖动 → 两个 region 都认为自己是主
- 数据分叉

**挑战 3 · 路由复杂度**
- CMDB 读时从哪个 region 读？就近？主 region？
- 需要路由表

**挑战 4 · 跨 region 写冲突**
- 同一实体在两个 region 同时被更新
- **依然靠 sync_version 乐观锁**

## §6.3 与阶段 4 的关系

阶段 5 **不是替代阶段 4，是叠加**：
- 同步机制仍是 Kafka + AOP + 乐观锁
- 只是数据源和目标都变成多 region
- Kafka 集群需要按 region 部署或跨 region 复制

---

# §7 · 阶段 6：FlinkCDC + AI 生成（2026·下一代）

## §7.1 战略动机

**为什么需要 FlinkCDC**：
- 阶段 4 需要**云产品方配合**（写 SPI + 生产 SyncMessage）
- 一些云产品**不愿意/不能配合**（团队人力不足、技术栈不匹配）
- FlinkCDC = **无侵入**（直接读他人 MySQL binlog，云产品完全不参与）

## §7.2 独特设计（iwiki 4024143170 明示，本人主导 2026 方案）

**关键设计原则**（可背，iwiki 原文）：
> ① DataSourceType 枚举**不新增 FLINK_CDC**，FlinkCDC 是模型上的 SyncMode（DIRECT / FLINK_CDC）
> ② 灰度以"脚本版本（ScriptVersion）+ CoveredRegions"为载体，不再有"批次（Batch）"
> ③ 一个模型最多 1 条 ACTIVE 脚本；旧 ACTIVE 在新版本保存时被置 SUPERSEDED（事务内）
> ④ **CMDB 不主动调用 Flink 平台，仅生成 SQL + 落库**；Flink 任务的提交/替换**由用户在 Flink 平台手动操作**

**核心决策**（面试杀手锏）：**CMDB 不主动管 Flink 任务**！只做三件事：
1. 生成 Flink SQL 脚本
2. 生成 CMDB 侧 sink 表 DDL
3. 落库保存脚本版本

**为什么这样设计**：**职责边界清晰**——CMDB 是"配置库"，Flink 是"计算平台"，两者松耦合。

## §7.3 DDL 关键表（iwiki 4024143170 完整定义）

**表 A · 扩展 `t_cmdb_custom_model_definition`**：
```sql
ALTER TABLE t_cmdb_custom_model_definition
  ADD COLUMN sync_mode VARCHAR(32) NOT NULL DEFAULT 'DIRECT'
    COMMENT '同步模式:DIRECT-实时直查 FLINK_CDC-订阅binlog落sink表',
  ADD COLUMN active_script_id BIGINT UNSIGNED NOT NULL DEFAULT 0,
  ADD COLUMN flink_sink_table VARCHAR(128) NOT NULL DEFAULT '';
```

**表 B · 扩展 `t_cmdb_custom_column_mapping`**：
```sql
ALTER TABLE t_cmdb_custom_column_mapping
  ADD COLUMN ignore_in_flink_cdc TINYINT NOT NULL DEFAULT 0
    COMMENT 'FlinkCDC同步时是否忽略该列:0-同步(默认) 1-忽略';
```

**表 C · 新建 `t_cmdb_custom_flink_cdc_script`**（脚本版本表）：
```sql
CREATE TABLE t_cmdb_custom_flink_cdc_script (
    id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    model_uid VARCHAR(128) NOT NULL,
    script_version INT NOT NULL,
    data_source_id BIGINT UNSIGNED NOT NULL,
    data_source_type VARCHAR(32) NOT NULL DEFAULT 'REGION_DB',
    source_db_name VARCHAR(128) NOT NULL,
    source_table VARCHAR(128) NOT NULL,
    source_pk VARCHAR(256) NOT NULL,
    sink_table VARCHAR(128) NOT NULL,
    cmdb_ddl MEDIUMTEXT,                     -- 完整 CREATE TABLE 语句
    cmdb_ddl_executed TINYINT DEFAULT 0,
    covered_regions VARCHAR(1024) NOT NULL,  -- csv
    gray_region_states JSON,                 -- 灰度状态
    ai_dialog_id BIGINT UNSIGNED,            -- ★ 与 TCUM-AI 关联
    flink_sql MEDIUMTEXT,                    -- 多 region UNION ALL 可达数十 KB
    flink_sql_hash VARCHAR(64) NOT NULL,     -- sha256 前后端一致性校验
    template_version VARCHAR(32) DEFAULT 'v1.0.0',
    lint_status VARCHAR(16) DEFAULT 'PENDING',
    lint_message VARCHAR(4000),
    script_status VARCHAR(16) DEFAULT 'DRAFT',
    parent_script_id BIGINT UNSIGNED,
    ...
);
```

**核心字段解读**：
- **`script_version` + `parent_script_id`**：版本树（可回溯演化）
- **`covered_regions`**：多 region 灰度粒度
- **`gray_region_states`**：JSON 里每个 region 的灰度状态
- **`lint_status`**：SQL Lint（PENDING/PASS/WARN/FAIL）
- **`script_status` 状态机**：**DRAFT → VALIDATED → ACTIVE → PAUSED → SUPERSEDED**

**表 D · `t_cmdb_custom_ds_parse_record`**（AI 数据源解析审计表）：
- **`ai_dialog_id` + `agent_name='tcum_cmdb_expert'` + `skill_name='cmdb-datasource-parse'`**
- **`llm_model / llm_token_in / llm_token_out / llm_cost_ms`**
- **AI 帮用户从 xlsx/csv/文本解析数据源配置**

## §7.4 AI 生成 FlinkCDC 脚本的独特价值

**场景**：用户接入一个新数据源
- **传统方式**：用户手写 Flink SQL（100~500 行）——门槛高
- **AI 方式**：用户上传数据源信息（xlsx / csv / 文本描述） → tcum_cmdb_expert Agent 解析 → 生成完整 Flink SQL + CMDB sink DDL

**AI 生成的 SQL 会走 Lint**（PENDING → PASS/WARN/FAIL），保证质量。

**这是 CMDB 与 TCUM-AI 深度对接的具体落地**——**面试可以主动讲**：
> "阶段 6 我们与 TCUM-AI 深度对接——用户上传数据源信息后，`tcum_cmdb_expert` Agent 通过 `cmdb-datasource-parse` skill 自动生成 Flink SQL 和 CMDB sink 表 DDL。整个过程 LLM token 消耗都会审计（`t_cmdb_custom_ds_parse_record`），并且脚本要走 Lint 校验才能 ACTIVE。"

## §7.5 提单注意事项（iwiki 原文明示的踩坑）

**坑点**（iwiki 4024143170 原文）：
> 1. 提单时请勾掉「动态检查」选项；ALTER 已改为**非幂等的直接 DDL**，仅在未变更过的库执行一次。
>    **原 PREPARE+INFORMATION_SCHEMA 的幂等写法被平台动态预检识别为 zichu.columns（库名错位）触发 1146 错误**。
> 2. flink_sql / cmdb_ddl 必须保留 MEDIUMTEXT：业务上需存储完整 Flink SQL 与建表 DDL（多 region UNION ALL 后可达 ~50KB）

**这是"工程实施踩坑"的教训** —— 面试可以讲：
> "我们在工蜂 SQL 审核平台踩过一个坑：原本想写幂等 DDL（PREPARE + INFORMATION_SCHEMA），但平台的动态预检把 information_schema.columns 识别成 zichu.columns（库名错位）触发 1146 错误。最后改成非幂等直接 DDL，只跑一次。"

## §7.6 单产品接入成本

**接近 0 人月**：
- 云产品完全不用参与
- 用户上传数据源 → AI 生成 Flink SQL → 灰度激活
- 主要成本转移到**Flink 集群运维**（团队负担）

## §7.7 阶段 6 的新问题

- Flink 状态大（每个模型一个 Flink 任务）
- 成本高（Flink 集群 vs Kafka 消费者）
- 运维复杂度（Flink 任务监控、checkpoint、savepoint）
- 他人 binlog 权限（需要跨部门审批）

---

# §8 · 演进曲线：单产品接入成本

```
接入成本 (人月)
   ▲
   │
5 ─┼──● 阶段1 单表模式
   │   │
   │   │
1 ─┼───●─── 阶段2 抽象元模型
   │   │  \
   │   │   \● 阶段3 直连 DB (表面下降,隐藏成本极高)
0.5 ─┼───────●── 阶段4 SPI+Kafka (本人主导, 真降本)
   │          \
   │           ●── 阶段5 多 region (基本持平)
0 ─┼─────────────●── 阶段6 FlinkCDC+AI (接近零)
   └──────────────────────────────────► 时间
     2020?    2023?    2025    2026
```

**面试价值**：**你可以完整讲这条曲线**——每个拐点你都能追问到细节。

---

# §9 · 与业界对比

## Netflix Edda（老牌 CMDB）
- 走的是**API polling 方案**（对应我们阶段 3）
- 无 event stream
- 演进停滞

## DataHub（LinkedIn / Acryl）
- **走的是 Kafka event streaming**（对应我们阶段 4）
- 支持 aspect versioning（类似我们的 sync_version）
- 但**没有 FlinkCDC 集成 + AI 生成**

## ServiceNow
- 老牌 ITSM 系统
- 走**Discovery Probe**（对应我们阶段 3+4 混合）
- 强 schema、静态定义（对应我们阶段 2）

## TCUM CMDB 的独特点
- **六阶段清晰演进** vs 其他家很多"一步做完"
- **阶段 6 与 AI 深度对接** —— 业界少见
- **script_status 状态机 + gray_region_states** —— 生产灰度经验

---

**本章完 · 约 25KB · 本卷最重头**

**面试要点回顾**：
1. **六阶段完整叙事**（3 分钟能讲完）
2. **阶段 4 是本人主导**（`unified_incr_sync_consumer.go` + INCR_SYNC_DESIGN.md 813 行）
3. **阶段 6 独特设计**：CMDB 不主动管 Flink 任务，只生成 SQL + DDL；与 AI 深度对接
4. **量化价值**：从 X 人月到接近 0

**下一章**：[`03-机制篇-增量同步核心.md`](./03-机制篇-增量同步核心.md) —— INCR_SYNC 完整解剖
