# 第二卷 · 面试话术与反问弹药

---

# §1 · 3 分钟自述稿

> "我在 TCUM CMDB 做了两年，负责的是**腾讯云内部所有云产品的统一元数据管理**。这个系统的特殊之处在于——**它不是一个单体应用，而是 11 个代码仓的组合体**：包括主 CMDB（cmdb + cmdb-core）、图数据库版（nebula_cmdb，基于 Nebula Graph 面向业务功能）、一致性校验（cmdb-check）、增量同步（tcum-cmdb-global，我主导的）、以及场景化的 paas-cmdb / xingyun-cmdb-op 等。
>
> **我最深度参与的是增量同步系统的设计与落地** —— 就是 `tcum-cmdb-global/INCR_SYNC_DESIGN.md`（本人 813 行设计文档）和 `unified_incr_sync_consumer.go`（272 行代码，`@author yaao`）。**核心设计有 4 点**：
> - **统一 Protobuf 消息协议 SyncMessage**（trace_id / operation / model_type / event_time / payload oneof）
> - **表驱动架构**（`model_type` → ModelRegistry → 表 + DO 类型，50+ 云产品实体各一个 service）
> - **AOP 版本控制**（乐观锁 = event_time.UnixMilli()，三钩子 BeforeCreate/Update/Delete，非侵入）
> - **最终一致性**（不用分布式锁，靠 sync_version + cmdb-check 巡检）
>
> **数据流向**：**云产品侧 → Kafka SyncMessage → UnifiedIncrSyncConsumer（enable.auto.commit=false 手动提交）→ AOP 版本检查 → DAO → MySQL**。这套机制解决了三个真实问题：**全量与增量并存的数据覆盖、消息乱序、多云产品接入成本**。
>
> **数据接入模式的演进有 6 个阶段**：单表 → 抽象元模型 → 直连他人 DB → SPI+Kafka（我主导的）→ 多 region → FlinkCDC + AI 生成（2026）。**接入成本从阶段 1 的 3~5 人月降到阶段 4 的 5~10 人日，阶段 6 接近零**。
>
> **短板必须坦白 3 条**：
> - 第一，**event_time 依赖数据源 NTP**，时钟漂移会导致新事件被跳过；
> - 第二，**SPI 无版本管理**，升级时会卡壳；
> - 第三，**权威源矩阵未代码化**——星云 CMDB 与公司 CMDB 冲突时靠约定。"

---

# §2 · 15 分钟深度稿

## 第 1 段（1 分钟）· 项目定位
（同 §1 前半）

## 第 2 段（4 分钟）· 数据接入六阶段演进（第 02 章）
- 单表 → 抽象 → 直连 DB → SPI+Kafka → 多 region → FlinkCDC + AI
- 每阶段"逼出来"和"新问题"
- **阶段 4 是本人主导**（INCR_SYNC_DESIGN.md）
- **阶段 6 独特设计**：CMDB 不主动管 Flink 任务

## 第 3 段（3 分钟）· 增量同步核心（第 03 章）
- SyncMessage Protobuf
- Kafka 消费者配置（enable.auto.commit=false, session.timeout.ms=10000）
- AOP 三钩子
- 三大冲突场景 + 乐观锁 SQL
- **at-least-once + 幂等 = 实用 exactly-once**

## 第 4 段（3 分钟）· 元模型与一致性（第 04 章）
- 11 仓组合体的分工哲学
- ModelRegistry 表驱动（50+ modelservice）
- cmdb-check 三模式 + 三响应
- 自定义模型 + AI 生成（阶段 6）

## 第 5 段（4 分钟）· 主动短板 + 演进方向（第 09/10 章）
- 5 条 P0 短板主动列
- FlinkCDC + AI 是下一代方向
- 与 TCUM-AI 深度对接

---

# §3 · 20 反问清单

## 组 1 · 架构与选型（5 问）

**Q1**. 你们的 CMDB 是单体还是多系统组合？团队怎么治理边界？
🔗 *我这边是 11 仓组合体。*

**Q2**. 图数据库 vs 关系型选型？读写比多少？
🔗 *我这边双写：MySQL 主库 + Nebula 只读图查询。*

**Q3**. 元数据 SPI 用什么协议？REST/gRPC/自定义？分页策略？
🔗 *我这边 REST + updated_since 分页；offset 分页有一致性坑，游标是改进方向。*

**Q4**. 多 CMDB 生态时（比如你们和公司 CMDB 并存）权威源怎么划分？
🔗 *我这边分维度权威源：设备 → 公司 CMDB / 业务 → 星云 CMDB。*

**Q5**. Flink CDC 在你们技术栈的位置？
🔗 *我这边 2026 演进方向，代码 DDL 已经准备好（t_cmdb_custom_flink_cdc_script）。CMDB 不主动调用 Flink 平台，只生成 SQL + DDL。*

## 组 2 · 数据一致性（5 问）

**Q6**. 增量同步用 Kafka 吗？at-least-once 还是 exactly-once？commit 策略？
🔗 *我这边 manual commit + 乐观锁幂等 = 实用 exactly-once。代码里 enable.auto.commit=false。*

**Q7**. 消息乱序怎么处理？版本号还是时间戳？依赖时钟怎么应对漂移？
🔗 *我这边 event_time 乐观锁，NTP 漂移是短板，考虑 ingest_time fallback 或 HLC。*

**Q8**. 全量与增量共存怎么保证不打架？
🔗 *我这边靠 sync_version 版本号。全量启动时的 syncVersion 是快照，增量后到的新数据 sync_version 更大 → 全量 UPDATE 跳过。*

**Q9**. 一致性巡检的策略是什么？发现大差异时怎么办？
🔗 *我这边三模式 + 三响应策略。差异 > 阈值 → 告警人工 → 拒绝自动修复。*

**Q10**. AOP 中间件的落地形态？拦截器/decorator/代理？
🔗 *我这边 AOP 三钩子（BeforeCreate/Update/Delete），一次开发全部生效。*

## 组 3 · 元模型（4 问）

**Q11**. 元模型的字段扩展策略？宽表 vs 写实 vs 属性表？
🔗 *我这边追求写实但存在字段膨胀，主字段 + JSON 混合是改进方向。*

**Q12**. 自定义模型怎么治理？

**Q13**. 实体的 owner 属性怎么维护？

**Q14**. 删除实体的语义？软删/硬删/归档？

## 组 4 · 拓扑与 AI（3 问）

**Q15**. 多跳查询的性能天花板？

**Q16**. 拓扑变更的推送机制？WebSocket/polling/event stream？

**Q17**. AI Agent 消费 CMDB 数据的接口设计？
🔗 *我这边 MCP 调用；t_cmdb_custom_ds_parse_record 审计 LLM token 消耗；tcum_cmdb_expert Agent + cmdb-datasource-parse skill。*

## 组 5 · 运营与治理（3 问）

**Q18**. CMDB 的接入方多少？SLA 多少？

**Q19**. 数据质量指标是什么？覆盖率/准确率/及时率？

**Q20**. CMDB 的 SLO 是什么？

---

**本篇完 · 约 6KB**

---

# 全卷索引

**CMDB 卷 12 篇合计**：

| # | 篇名 | 定位 |
|---|---|---|
| 00 | 索引与使用说明 | 全卷入口 |
| 01 | 机制篇 A · 11 仓组合体架构 | 边界与协作 |
| **02** | **机制篇 B · 数据接入演进史** | **本卷灵魂章** |
| 03 | 机制篇 C · 增量同步核心 | 本人代码解剖 |
| 04 | 机制篇 D · 一致性巡检与元模型 | cmdb-check + 自定义模型 |
| 05 | 场景篇 · 真实变更端到端推演 | 3 个 case |
| 06 | 30 问对比 A · 架构与数据同步 | 12 题 |
| 07 | 30 问对比 B · 元模型与一致性 | 12 题 |
| 08 | 30 问对比 C · 拓扑与 AI | 6 题 |
| 09 | 短板矩阵与自曝清单 | 15+ 短板 |
| 10 | 优化编年史 · 问题驱动 | 11 条演进 |
| 11 | 面试话术与反问弹药 | 本文 |

**合计约 130 KB · 面试宝典第二卷完整交付**
